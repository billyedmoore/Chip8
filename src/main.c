/**
 * Should have the main loop.
 *
 * Each cycle should:
 *  cycle(system*)
 *  handle_keypr(system*)
 *  draw()
 */
#include "cpu.h"

#include <stdio.h>
#include <string.h>

int main() {
  Chip8 *sys = systemInit();
}
